# Test-only dependencies. Kept separate from requirements.txt so the
# production runtime image stays slim. Install with:
#     pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0
pytest-cov>=5.0.0
pyyaml>=6.0
httpx>=0.27.0   # also a runtime dep — pinned here for fastapi.testclient
